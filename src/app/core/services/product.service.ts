import { Injectable } from '@angular/core';

/**
 * @deprecated Use AdminProductsService from features/products/services instead.
 * This service previously held hardcoded mock data and is no longer used.
 * Retained only to avoid breaking barrel exports; safe to delete once confirmed unused.
 */
@Injectable({
  providedIn: 'root'
})
export class ProductService {}
